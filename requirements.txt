# AI Debate Arena — Python dependencies
# Spec 04 §5: pin exact versions once local dev is confirmed working, so HF Spaces
# Docker builds don't break on a floating-version change. Until then, this list is
# intentionally unpinned for easy iteration. Requires Python >=3.11, <3.14
# (CrewAI itself requires >=3.10, <3.14; AGENT.md §3 targets 3.11+).

fastapi
uvicorn[standard]
crewai
python-dotenv
