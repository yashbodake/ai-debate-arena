# AI Debate Arena — Python dependencies.
# Pinned to exact versions verified working (Spec 04 §5: pin once local dev is
# confirmed, to avoid "works locally, breaks on Space" build failures).
# Requires Python >=3.11, <3.14 (CrewAI constraint; AGENT.md §3 targets 3.11+).
fastapi==0.139.0
uvicorn[standard]==0.50.2
crewai==1.15.1
python-dotenv==1.2.2
